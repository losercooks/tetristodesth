
# 俄罗斯方块小游戏框架架构分析

本文档旨在通过架构图和文字分析，阐述当前游戏框架的结构、功能、优缺点，以便于后续的开发和维护。

## 1. 架构图

我们采用分层架构，将UI、业务逻辑和底层服务清晰地分离开来。这有助于降低模块间的耦合度，提高代码的可读性和可扩展性。

```mermaid
graph TD
    subgraph 小程序视图层 (View)
        A[pages/index/index.wxml - 首页视图]
        B[pages/game/game.wxml - 游戏视图]
        C[pages/scores/scores.wxml - 排行榜视图]
    end

    subgraph 小程序逻辑层 (Controller)
        D[pages/index/index.js - 首页逻辑]
        E[pages/game/game.js - 游戏页逻辑]
    end

    subgraph 游戏引擎核心 (Engine Core)
        F[Game.js - 游戏主循环/协调器]
        subgraph UI模块
            G[Renderer.js - 渲染器]
            H[widgets/Button.js - UI组件]
            I[SceneManager.js - 场景管理]
        end
        subgraph 游戏逻辑模块
            J[GameLogic.js - 核心玩法]
            K[Tetris.js - 方块控制]
            L[Board.js - 棋盘状态]
            M[Score.js - 分数系统]
        end
    end

    subgraph 底层服务 (Services & Utils)
        N[InputManager.js - 输入处理]
        O[AudioPlayer.js - 音频播放]
        P[StateManager.js - 状态管理]
    end

    subgraph 网络模块 (Network)
        Q[RoomManager.js - 房间/匹配管理]
        R[SocketIO.js - 实时通信]
    end

    A -- interacts with --> D
    B -- interacts with --> E
    D -- starts --> F
    E -- creates and runs --> F

    F -- uses --> G
    F -- uses --> J
    F -- uses --> N

    J -- controls --> K
    J -- updates --> L
    J -- updates --> M

    F -- can use --> Q
    Q -- uses --> R

    G -- renders on --> B
```

### 架构解读：

1.  **视图层 (View)**: 完全由小程序原生的 `.wxml` 和 `.wxss` 文件构成，负责界面的展示。游戏的核心画面由 `pages/game/game.wxml` 中的 `<canvas>` 承载。
2.  **逻辑层 (Controller)**: 小程序 `pages` 目录下的 `.js` 文件，作为连接视图层和游戏引擎的桥梁。它们负责响应页面的生命周期事件（如 `onLoad`），创建和启动游戏引擎 (`Game.js`)。
3.  **游戏引擎核心 (Engine Core)**: 这是整个游戏的核心，所有与游戏玩法直接相关的逻辑都在这里。
    *   **游戏主循环 (`Game.js`)**: 负责启动、暂停、结束游戏，并在每一帧（通过 `requestAnimationFrame`）调用渲染器和游戏逻辑更新，是所有模块的“指挥官”。
    *   **UI模块 (`/src/ui`)**: 负责在 `<canvas>` 上绘制所有内容，包括游戏元素（方块、背景）和UI控件（按钮、对话框）。它不关心游戏逻辑，只负责“画”。
    *   **游戏逻辑模块 (`/src/core`, `/src/gamelogic`)**: 纯粹的逻辑实现，如方块如何移动、旋转、碰撞，如何计分，如何消除行。它不关心如何绘制，只负责计算和维护游戏状态。
4.  **底层服务 (Services & Utils)**: 提供与具体玩法无关的通用能力，如监听用户触摸/键盘输入 (`InputManager`)、播放声音 (`AudioPlayer`) 等。
5.  **网络模块 (Network)**: 一个预留的、独立的模块，专为实现双人PK或在线功能而设计。它负责玩家匹配和实时数据同步。

## 2. 功能与能力

*   **单人模式**: 框架已完全支持单人积分模式的开发。`Score.js` 负责计分，`GameLogic.js` 驱动玩法。
*   **排行榜**: 具备与云端交互的能力。游戏结束后，可将分数上传至小程序云开发数据库；排行榜页面可从数据库拉取数据展示。
*   **双人PK (可扩展)**: 通过预留的 `network` 模块，框架具备扩展为实时网络对战的能力。当一个玩家消除行时，可以通过 `SocketIO.js` 发送消息，在对手的 `Board.js` 中增加“垃圾行”。
*   **跨平台**: 基于小程序，天然具有跨 iOS 和 Android 平台的能力。
*   **性能**: 采用 `canvas` 进行游戏渲染，相比于操作大量 WXML 节点，性能更高，更适合游戏场景。

## 3. 优缺点分析

### 优点：

1.  **高内聚，低耦合**: 职责划分清晰。修改游戏逻辑（如新增一种方块）不会影响渲染代码；更换UI（如按钮样式）也不会影响游戏逻辑。这使得代码极易维护。
2.  **可扩展性强**: 无论是想增加新的单人玩法，还是开发双人PK模式，甚至是接入更复杂的后端服务，当前的架构都提供了清晰的扩展点。
3.  **可复用性高**: `widgets` 下的UI组件、`utils` 下的工具类，以及 `core` 中的核心逻辑，都可以在未来的其他游戏中被复用。
4.  **易于测试**: 由于逻辑和渲染分离，可以针对 `GameLogic`、`Board` 等核心模块编写独立的单元测试，确保游戏玩法的正确性。

### 缺点：

1.  **上手门槛稍高**: 对于初学者或非常简单的项目，这套分层架构可能显得有些“重”。需要理解模块划分和数据流转的方式。
2.  **UI开发复杂性**: 所有UI都绘制在 `canvas` 上，这意味着无法享受 WXML 和 CSS 带来的便利（如自动布局、CSS动画、丰富的组件库）。`canvas` UI 的开发和调试成本更高。
3.  **状态管理**: 对于更复杂的游戏（例如，带有多种道具、复杂计分规则），`StateManager.js` 可能需要升级为更成熟的状态管理模式（如Redux或事件总线），以避免逻辑混乱。

## 4. 实战演练：俄罗斯方块从0到1的30个步骤

*以下是从项目准备到最终发布的完整操作路线图。其中涉及“服务器”和“WebSocket”的步骤，均统一指向使用**方案A：微信云托管**来实现。*

### 第一阶段：项目准备与资质攻坚 (共 4 步)

1.  **注册游戏小程序**：在微信公众平台注册账号，服务类目选择“游戏 → 休闲游戏”。
2.  **下载开发者工具**：安装官方的“微信开发者工具（Stable稳定版）”，新建项目选择“小游戏”。
3.  **软著申请与内容自审**：去版权局或者找代办申请《俄罗斯方块[暂定名]软件著作权登记证书》，并撰写《游戏自审自查报告》（上线备案必须）。
4.  **服务器部署 (选择云托管)**：在微信公众平台开通**微信云托管**服务，并创建一个服务（如 `tetris-battle-service`），为后续的联机对战做准备。

### 第二阶段：游戏核心逻辑层(Data Core)开发 (共 6 步)

5.  **初始化 10x20 地图 (Board.js)**：用一个 10 × 20 的二维数组代表网格，0 代表空格，1-7 代表被锁死的七彩方块。
6.  **定义 7 种方块 (Tetromino.js)**：用二维矩阵写出 I、O、T、J、L、S、Z 形状。注意：按照 NES 规则，I、S、Z 只有两种旋转状态。
7.  **编写碰撞检测算法 (collide)**：输入方块未来的坐标和形状，判定是否越出左/右/下边界，或者是否与地图上已有方块（数组不为0的格子）重叠。
8.  **编写消行与平移逻辑 (checkLines)**：由下至上检查满行，使用 `splice` 剔除满行，并用 `unshift` 在大数组顶部压入空行，返回本次消行数量（0-4）。
9.  **编写方块变形旋转 (Rotate)**：编写矩阵顺时针顺转算法，由于 NES 没有“踢墙”机制，旋转时如果发生 `collide` 碰撞，则直接拒绝本次旋转。
10. **封装主状态机 (GameLogic.js)**：组合上述模块。实现 `moveDown()`、`moveLeft()`、`moveRight()`。逻辑为：“先预测移动 → 碰撞检测 → 没撞则更新坐标 → 向下撞了则触发 lock（固化方块 → 触发消行 → 判断顶出 TopOut 游戏结束 → 刷新新方块）”。

### 第三阶段：原生 UI 与 Canvas 渲染层开发 (共 6 步)

11. **屏幕适配 (Viewport.js)**：通过 `wx.getSystemInfoSync` 获取手机屏幕实际宽高。定死一个设计分辨率（如 750×1334），计算出缩放比例，并计算出 10x20 舞台格子在各种屏幕下居中所占的像素大小（如每个格子 30px）。
12. **基础组件封装 (widgets/)**：手动用代码封装 `Button.js`（含 x, y, w, h 和坐标点击判定）和 `Text.js`（字号、对齐）。
13. **场景管理器 (SceneManager.js)**：用一个 `currentScene` 状态变量，通过 `switch-case` 控制屏幕擦除后应该画哪个场景：主菜单、单人游戏、房间页、PK页、结算页。
14. **开发全局事件分发 (EventDispatcher.js)**：绑定 `wx.onTouchStart` 和 `wx.onTouchEnd`。当玩家点击屏幕，获取 `clientX`, `clientY` 坐标，先乘以适配比例，再分发给当前激活场景的各个 Button。注意：遍历多点触控（`e.touches`）确保双手操作不冲突。
15. **实现长按连续移动 (DAS系统)**：在触摸逻辑中，加入计数器和定时器。实现“按下瞬间移一格 → 停顿 260ms → 只要不松手，每隔 100ms 快速连续移动”的 NES 传统手感。
16. **搭建像素级 Canvas 渲染器 (Renderer.js)**：设置高频主循环 `requestAnimationFrame`。每帧先 `clearRect` 清空画布，然后根据场景绘制 UI。如果是单人游戏场景，把 10x20 数组中非 0 的格子画成彩色方块，0 的格子画成半透明网格线。

### 第四阶段：单人排行与微信关系链 (共 4 步)

17. **创建独立“开放数据域”**：在项目根目录新建一个独立文件夹（如 `openDataContext`），在 `game.json` 中配置指向它。
18. **最高分云端上报**：在单人游戏结算时，主域调用 `wx.setUserCloudStorage` 将玩家的最高分数和关卡等级（Level）上传至微信后台服务器。
19. **开放数据域拉取好友榜**：在开放数据域的 `index.js` 中调用 `wx.getFriendCloudStorage`。微信会安全返回所有玩过此游戏的好友的微信头像、昵称和上传的分数。
20. **主域与子域画布拼接**：在开放数据域内用代码把好友列表（头像、名次、分数）画在它独立的 `SharedCanvas` 上；当玩家在主菜单点击“排行榜”时，主域把这张 `SharedCanvas` 当成一张大图片画在弹窗正中央。

### 第五阶段：网络对战与好友 PK 层 (共 6 步)

21. **自建 WebSocket 服务端 (使用云托管)**：使用 Node.js + Socket.io 编写对战服务器逻辑，并将其部署到**微信云托管**。
22. **小游戏合法域名配置**：将**云托管**提供的服务域名，填入微信公众平台后台的“服务器域名白名单”中。
23. **前端网络封装 (SocketIO.js)**：在小游戏主域调用 `wx.connectSocket` 与你的**云托管**服务建立长连接，监听并发送联机消息。
24. **实现微信“好友邀请裂变”**：在房间页面，点击“邀请好友”按钮，调用 `wx.shareAppMessage`，在转发分享的链接中注入当前的房间号参数（如 `roomID=1024`）。当好友点击这个微信分享卡片启动游戏时，在 `wx.onShow` 中捕获该 `roomID`，并自动通过 WebSocket 塞进同一个对战房间。
25. **双画面同屏渲染**：进入 PK 模式场景。左边大区域渲染本地玩家 1 的 10x20 舞台；右边以 50% 比例缩小，渲染通过网络每帧传过来的玩家 2 的 10x20 地图数据。
26. **对战攻防垃圾行逻辑**：本地玩家 1 触发消行时（Double消2行/Triple消3行/Tetris消4行），通过网络向对方发送攻击指令。对方收到后，在当前活动方块锁死时，在对方 `Board` 地图的最底部强行插入 1/2/4 行带有随机缺口的灰色方块（垃圾行），把对方往顶部顶。

### 第六阶段：润色、合规与提审发布 (共 4 步)

27. **音频与动效接入**：使用 `wx.createInnerAudioContext` 加载 `assets/audio/` 中的音效。接入方块落地声、消行爆炸声、NES 经典的背景 BGM，并在消行时让对应网格闪烁 3 帧。
28. **线上小程序备案**：在微信小程序管理后台提交“小程序备案”，填写主体信息，进行人脸核验。（注意：目前备案审核需要 1-2 周，必须提前做）。
29. **上传代码与测试**：在微信开发者工具点击“上传”，将其作为“开发版本”。在后台生成“体验版二维码”，发送给朋友进行真机联机实测，修复 Bug。
30. **正式提交审核**：在后台将开发版本转为“审核版本”，提交游戏自审报告，等待微信官方代码审核。 IAA（纯广告变现）通常需要 13-37 个工作日。审核通过且备案完成后，点击“发布”，小游戏正式面向全国微信用户上线！

**因此，我们的项目将采用方案A：使用微信云托管。**

## 5. 目录框架

├── miniprogram/                // 微信小游戏根目录
│   ├── game.js                 // 游戏唯一总入口文件（初始化微信环境）
│   ├── game.json               // 小游戏全局配置文件（配置横竖屏、分包等）
│   ├── project.config.json     // 微信开发者工具配置文件
│   │
│   ├── assets/                 // 静态资源目录（图片、音效）
│   │   ├── images/             // 方块皮肤、UI背景、按钮切图
│   │   └── audio/              // 消除音效、背景音乐、落底音效
│   │
│   └── src/                    // 核心代码目录
│       ├── main.js             // 游戏主控制类（主循环 requestAnimationFrame）
│       │
│       ├── core/               // 【俄罗斯方块核心逻辑（纯数据层，不含UI）】
│       │   ├── Board.js        // 游戏地图网格（10x20矩阵，处理消行、固定方块）
│       │   ├── Tetromino.js    // 七种方块定义（I, J, L, O, S, T, Z 旋转逻辑）
│       │   └── GameLogic.js    // 核心玩法控制器（下落速度、得分计算、游戏结束判定）
│       │
│       ├── ui/                 // 【UI 与 场景架构】
│       │   ├── SceneManager.js // 场景切换（主菜单 <-> 单人 <-> PK模式 <-> 结算）
│       │   ├── Viewport.js     // 屏幕适配（全面屏留白、计算方块渲染的格子大小）
│       │   ├── Renderer.js     // Canvas 渲染器（负责把 Board 里的数据画成方块）
│       │   └── widgets/        // 自定义 UI 控件
│       │       ├── Button.js   // 虚拟按键（左移、右移、变形、急速下落）
│       │       └── Dialog.js   // 弹窗（暂停、游戏结束提示）
│       │
│       ├── network/            // 【网络与对战模块（好友PK核心）】
│       │   ├── SocketIO.js     // 微信 WebSocket 封装（与对战服务器长连接）
│       │   └── RoomManager.js  // 房间管理（创建房间、加入房间、好友邀请裂变）
│       │
│       ├── openDataContext/    // 【开放数据域（微信好友排行榜专区，独立包）】
│       │   ├── index.js        // 开放数据域入口
│       │   └── render.js       // 使用 wx.getFriendCloudStorage 绘制好友排行榜 UI
│       │
│       └── utils/              // 【工具类】
│           ├── AssetLoader.js  // 资源加载器
│           └── EventBus.js     // 全局事件总线（用于模块间通信，如消行时通知UI加分）



这是一个专业、健壮且可扩展的游戏框架，非常适合用于开发具有一定复杂度的俄罗斯方块小游戏，并为未来的功能迭代（如在线对战）打下了坚实的基础。虽然 `canvas` 增加了UI开发的复杂度，但它带来的性能优势对于保证游戏流畅体验是至关重要的。
