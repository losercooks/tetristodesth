# 2D 小游戏开发框架：方法论与工具箱

## 1. 核心方法论：分层架构与关注点分离

本框架的核心思想是采用**分层架构 (Layered Architecture)**，将一个游戏项目清晰地划分为多个低耦合的独立层面。这种**关注点分离 (Separation of Concerns)** 的方法论可让您在开发时专注于单一职责，极大地提高代码的可维护性和扩展性。

我们将项目解构为以下几个核心分层：

-   **视图层 (View Layer)**: 负责UI的渲染与展示，是用户直接交互的界面。在小程序中，它主要由 `.wxml` 文件和用于高性能绘图的 `<canvas>` 元素构成。
-   **控制器层 (Controller Layer)**: 作为视图层与游戏引擎之间的“胶水层”。它由小程序页面的 `.js` 文件组成，负责监听页面的生命周期和用户事件，并调用游戏引擎的相应接口。
-   **引擎/逻辑层 (Engine/Logic Layer)**: 这是游戏的核心，位于 `/src` 目录。它包含所有游戏的状态数据、核心算法和玩法规则。这一层是纯粹的数据与逻辑处理，完全独立于视图，使其拥有高度的可移植性和可测试性。
-   **服务/工具层 (Services/Utils Layer)**: 提供项目所需的通用功能与底层服务。例如输入管理、音频播放、网络通信等，为上层模块提供支持。

## 2. 工具箱：可复用的核心组件

-   **`Game.js` (游戏协调器)**: 负责启动、暂停、结束游戏，并以固定的频率（`requestAnimationFrame`）刷新游戏。
-   **`SceneManager.js` (场景管理器)**: 管理游戏不同场景（主菜单、游戏、结算）的切换。
-   **`Renderer.js` (渲染器)**: 负责在 `<canvas>` 上根据数据模型进行绘图。
-   **`/src/ui/widgets` (UI组件库)**: 提供了在 Canvas 上绘制按钮、对话框等UI元素的能力。
-   **`InputManager.js` (输入管理器)**: 统一管理用户的触摸、点击等操作。
-   **`AudioPlayer.js` (音频播放器)**: 用于播放背景音乐和音效。
-   **`/src/network` (网络服务模块)**: 封装网络通信，实现客户端与后端的交互。

## 3. 使用指南：如何打造您的游戏

### 模式一：单人游戏与排行榜 (使用云开发)

对于非实时、请求响应式的模式，使用“微信云开发”依然是最高效的选择。

**步骤：**

1.  **定义核心玩法**: 在 `/src/core` 创建您的游戏对象和规则。
2.  **实现视觉渲染**: 修改 `Renderer.js` 以绘制您的游戏对象。
3.  **集成排行榜**: 游戏结束时，调用一个**云函数**（如 `updateScore`），将分数安全地写入**云数据库**。排行榜页面则调用另一个云函数（如 `getLeaderboard`）来读取数据。

### 模式二：双人实时PK

对于俄罗斯方块这种需要低延迟（<50ms）的实时对战，后端必须通过 **WebSocket** 实现持久连接。这里我们提供两种主流的后端实现方案。

## 针对核心功能的深度解析与避坑指南

### 1. 单人积分与排行榜（开放数据域 openDataContext）
- **微信限制**：为了保护用户隐私，微信严禁在游戏主域直接获取好友的头像和名字。你必须通过一个独立的文件夹（即上面的 openDataContext）来处理排行榜。
- **实现原理**：玩家单人游戏结束，在主域调用 wx.setUserCloudStorage() 把最高分上传到微信服务器。点击排行榜按钮时，主域向开放数据域发送消息。开放数据域内部调用 wx.getFriendCloudStorage() 捞取好友分数，并在开放数据域的独立 Canvas 上画出排行榜，最后作为一张贴图“盖”在主域的屏幕上。

### 2. 好友之间 PK（双人实时对战）
- **不能用微信自带的云开发数据库**：云数据库（Cloud DB）是轮询或监听机制，延迟高达几百毫秒，无法满足俄罗斯方块双人对战中“你消一行，我涨一行”的实时性要求。
- **正确做法**：你需要自己用 Node.js（配合 Socket.io）或者使用现成的游戏联机引擎（如腾讯云 MGOBE 类似的服务，注：MGOBE已停服，目前行业多用网易有球或自建 WebSocket 服务器）。
- **对战同步数据极小**：不需要实时同步每个格子的移动！为了防作弊和降低延迟，网络包只需同步：
    - **操作指令同步**：玩家 A 按了“左移/右移/旋转/下落”。
    - 或 **状态同步**：玩家 A 产生了“消行”，向服务器发送 sendGarbageLines(count)，服务器立刻通知玩家 B 的游戏逻辑在底部生成对应数量的灰色垃圾行。

### 3. 原生 UI 的双人画面适配（Renderer.js）
- **单人模式**：方块主舞台居中，两侧或底部放分数值、下一个方块（Next）提示、虚拟摇杆。
- **PK 模式**：由于是竖屏，屏幕将水平等分成左、右两个半区。左边渲染你自己的 Board 矩阵，右边渲染通过 WebSocket 实时传过来的对手的 Board 矩阵（通常等比例缩小，只做画面呈现）。

---

#### 方案A：使用微信云托管 (官方推荐)

**简介**：微信云托管 (CloudRun) 是微信官方提供的一站式后端云服务。它允许您直接部署代码容器，并自动处理服务器运维、弹性伸缩和网络加速，是目前微信小游戏联机对战的**最佳实践**。

**步骤：**

1.  **开通云托管服务**
    *   登录微信公众平台后台，在左侧菜单栏找到 “开发” -> “开发管理” -> “微信云托管”，点击开通并绑定你的小游戏账号。
    *   在云托管控制台中创建一个“服务”（例如命名为 `tetris-battle-service`），选择按量计费。

2.  **编写 WebSocket 后端服务 (Node.js + Socket.io)**
    *   在项目根目录创建一个新的文件夹 `server`，用于存放我们所有的后端代码。
    *   在该文件夹中创建 `package.json` 和 `index.js`，使用 `socket.io` 搭建 WebSocket 服务器，编写核心联机逻辑。

3.  **配置并一键部署**
    *   在 `server` 文件夹中，创建一个 `Dockerfile` 文件，用于定义服务的运行环境。
    *   在微信开发者工具中，切换到“云托管”面板，选择我们创建的服务，上传 `server` 文件夹即可完成部署。云托管会自动构建镜像并启动服务。

4.  **客户端连接**
    *   在小程序管理后台，将云托管生成的默认域名配置到“服务器域名白名单”中。
    *   修改 `/src/network/SocketIO.js`，调用 `wx.connectSocket` 连接到云托管的 `wss://` 地址。

---

#### 方案B：使用自建云服务器 (传统方案)

**简介**：此方案指自行购买和管理一台云服务器（如腾讯云CVM、阿里云ECS），并在上面手动搭建和维护 WebSocket 服务。

**步骤：**

1.  **购买与配置服务器**
    *   购买一台云服务器，并安装操作系统（如 `Ubuntu`）。
    *   安装 `Node.js`、`Nginx` 等必要的环境软件。
    *   购买一个域名，并申请/配置 SSL 证书以支持 `https://` 和 `wss://`。

2.  **编写并部署 WebSocket 服务**
    *   后端代码的编写与方案A完全相同（Node.js + Socket.io）。
    *   将代码上传到您的服务器，并使用进程守护工具（如 `PM2`）来启动并管理您的服务，确保其能7x24小时不间断运行。
    *   配置 Nginx 进行反向代理，将来自 `wss://your-domain.com` 的请求转发到您本地运行的 Node.js 进程端口。

3.  **客户端连接**
    *   在小程序管理后台，将您自己的服务器域名配置到“服务器域名白名单”中。
    *   修改 `/src/network/SocketIO.js`，调用 `wx.connectSocket` 连接到您自己的 `wss://your-domain.com` 地址。

---

### 方案对比：云托管 vs. 自建服务器

| 对比维度 | ✅ 微信云托管 (CloudRun) | ⚠️ 自建云服务器 |
| :--- | :--- | :--- |
| **运维成本** | **几乎为零**。免运维，自动扩缩容，故障自愈。 | **极高**。需手动配置、监控、打补丁、处理攻击、考虑扩容。 |
| **开发效率** | **高**。与开发者工具深度集成，一键部署，无需关心底层。 | **低**。需要大量时间进行服务器环境配置和部署调试。 |
| **初期成本** | **低**。按量计费，有免费额度，前期几乎无成本。 | **固定成本**。无论有无用户，都需要按月支付服务器费用。 |
| **弹性伸缩** | **自动**。根据用户访问量自动增减实例，从容应对流量洪峰。 | **手动**。需要预估流量，手动升降配置，或自行搭建复杂的扩容架构。 |
| **灵活性** | 较低。平台环境相对固定，无法操作系统底层。 | **极高**。拥有服务器的最高权限，可进行任意定制和优化。 |

**结论：**

-   对于**绝大多数开发者、中小型团队以及初创项目**，**微信云托管**是无可争议的最优选择。它极大地降低了后端开发和运维的门槛与成本，让您可以更专注于游戏玩法本身。
-   **自建云服务器**仅适用于拥有**专业运维团队**，且对服务器有深度定制化需求的大型项目。
